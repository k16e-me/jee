---
import Text from '@atoms/Text.astro'

const {
	text,
	level,
	underline = true,
	size,
	class: classes,
	...props
} = Astro.props
---

<div
	class:list={[
		'',
		underline &&
			'border-b border-[var(--color-border-accent)] pb-1.5 md:pb-2.5',
		classes,
	]}
	{...props}>
    <Text
        tag={level}
        text={text}
        color='title'
        size={size}
        family='display'
        weight='bold'
        tighten
        class=''
    />
</div>
