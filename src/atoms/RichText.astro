---
import { renderRichText } from '@storyblok/astro'
import Prose from '@atoms/Prose.astro'

const {
		text,
		class: classes,
		...props
	} = Astro.props
---

<Prose
	class:list={[
		classes,
	]}
	{...props}>
	<Fragment set:html={ renderRichText(text) } />
</Prose>
