---
import Icon from '@atoms/Icon.astro'

const {
	link,
	label,
	icon,
	style = 'default',
	flipIcon,
	class: classes,
	...props
} = Astro.props
---

<button
	class:list={[
		'w-10 h-10 flex items-center justify-center group rounded-full',
		classes,
		style === 'default' &&
			'bg-[var(--color-bg-default-elevated)] text-[var(--color-text-emphasis)]',
		style === 'blank' && 'bg-transparent text-[var(--color-text-body)]',
		style === 'blank-brand' && 'bg-transparent text-[var(--color-text-emphasis)]',
		style === 'brand' && 'bg-[var(--color-emphasis)] text-white',
	]}
	{...props}
	aria-label={label}>
	<Icon
		name={icon}
		class:list={[
			'w-[22px] text-inherit group-hover:scale-110 block',
			flipIcon && 'rotate-90',
		]}
	/>
</button>
